---

---

<footer
    class="border-t border-gray-200 pt-12 mt-12 grid grid-cols-1 md:grid-cols-3 gap-8 text-sm"
>
    <div>
        <h4 class="font-mono font-bold uppercase mb-4">Rocco Andrea Ligorio</h4>
        <p class="font-serif text-gray-600">
            Consulente, formatore e divulgatore.<br />
            Smonto la tecnologia pezzo per pezzo.
        </p>
    </div>
    <div>
        <h4 class="font-mono font-bold uppercase mb-4">Mappa Sito</h4>
        <ul class="space-y-2 font-mono text-gray-600">
            <li>
                <a href="/articoli" class="hover:underline"
                    >Tutti gli Articoli</a
                >
            </li>
            <li>
                <a href="/servizi" class="hover:underline">Servizi Tech</a>
            </li>
            <li>
                <a href="/news" class="hover:underline">News Feed</a>
            </li>
            <li>
                <a href="/contatti" class="hover:underline">Canale SOS</a>
            </li>
        </ul>
    </div>
    <div>
        <h4 class="font-mono font-bold uppercase mb-4">Legal</h4>
        <li class="text-xs text-gray-400 mt-2">
            © 2025 Rickettsia Studio.<br />
            Nessun cookie tracciante.<br />
            Solo HTML artigianale.
        </li>
        <li class="mt-4">
            <button
                id="admin-login-btn"
                class="font-mono text-xs text-gray-400 hover:text-ink uppercase tracking-widest border border-gray-200 px-2 py-1 rounded hover:border-ink transition-all"
            >
                /// Admin Access
            </button>
        </li>
    </div>
</footer>

<script>
    // Handle Admin Login Click securely
    const btn = document.getElementById("admin-login-btn");
    if (btn) {
        btn.addEventListener("click", () => {
            if (window.netlifyIdentity) {
                window.netlifyIdentity.open();
            } else {
                console.error("Netlify Identity script not loaded yet.");
                alert("Attendere caricamento sistema di sicurezza...");
            }
        });
    }

    // Auto-detect invitation token from email
    if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", (user) => {
            if (!user) {
                window.netlifyIdentity.on("login", () => {
                    document.location.href = "/admin/";
                });
            }
        });
    }
</script>

<!-- Force load Identity Widget -->
<script
    is:inline
    async
    src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
